"""initial control-plane schema

Baseline migration. Creates all control-plane tables from the app metadata so the
schema has a single source of truth (``app.db.tables``). Subsequent migrations
should be explicit/autogenerated diffs against that metadata.

Revision ID: 0001
Revises:
Create Date: 2026-06-09
"""

from __future__ import annotations

from alembic import op

from app.db.tables import metadata

revision = "0001"
down_revision = None
branch_labels = None
depends_on = None


def upgrade() -> None:
    metadata.create_all(bind=op.get_bind())


def downgrade() -> None:
    metadata.drop_all(bind=op.get_bind())
