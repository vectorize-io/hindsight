# Operator GUI config. Copy to .env.local. NO secrets here.
# Base URL of the CollabMind Central API. The GUI calls ONLY this — never Google
# Drive, Qdrant, or the database directly.
NEXT_PUBLIC_CENTRAL_API_URL=http://localhost:8000
