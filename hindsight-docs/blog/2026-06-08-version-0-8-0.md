---
title: "What's new in Hindsight 0.8.0"
description: Cross-instance bank migration, LLM request tracing on by default, self-maintaining consolidation, and resumable long-running operations in Hindsight 0.8.0
authors: [nicoloboschi]
date: 2026-06-08
hide_table_of_contents: true
tags: [release]
---

Hindsight 0.8.0 is a major release focused on **running memory at scale and seeing inside it**. Banks can now move between instances with a built-in export/import, every LLM call behind retain, recall, and reflect is traced out of the box, consolidation keeps itself clean and bounded without supervision, and long-running operations survive a restart instead of starting over. Along the way the memory history layer was rebuilt to stop unbounded growth, and provider prompt caching cuts the cost and latency of the LLM calls Hindsight makes on your behalf.

<!-- truncate -->

- [**Cross-Instance Bank Migration**](#cross-instance-bank-migration): Export a whole bank — or just its documents — and import it into another Hindsight instance, re-embedding locally with no LLM re-run.
- [**LLM Request Tracing**](#llm-request-tracing): Per-bank tracing of every LLM call, on by default, viewable in the Control Plane and the SDKs.
- [**Self-Maintaining Consolidation**](#self-maintaining-consolidation): Semantic de-duplication of observations on by default, plus a background loop that reconciles and applies retention on its own.
- [**Resumable Long-Running Operations**](#resumable-long-running-operations): Consolidation and batch retain checkpoint their progress and pick up where they left off after a restart.
- [**Scalable Memory History**](#scalable-memory-history): Mental-model and observation history moved into dedicated tables, removing an unbounded-JSONB growth ceiling on large banks.
- [**Provider Prompt Caching**](#provider-prompt-caching): Prompt-prefix caching across retain, consolidation, and reflect — default on — to cut token cost and latency.

## Cross-Instance Bank Migration

Moving a memory bank from one Hindsight deployment to another — staging to production, self-hosted to Cloud, or between regions — used to mean re-ingesting the source content and paying for every extraction again. 0.8.0 adds first-class migration.

- **Whole-bank export/import.** The `hindsight-admin export-bank` command packages a complete bank — memory units, mental models, entities, links, configuration, and mental-model history — into an archive, and `import-bank` restores it into another instance. The destination **re-embeds locally** against its own embedding model, so no LLM extraction is re-run and the migration is fast and cheap.
- **Document transfer.** A dedicated document-transfer API endpoint moves a bank's source documents between banks without re-running the LLM, so you can rebuild or fork a bank from its originals.

Because the destination re-embeds on import, you can migrate between instances even when they run different embedding back-ends.

## LLM Request Tracing

Hindsight makes a lot of LLM calls on your behalf — fact extraction during retain, query analysis and reranking during recall, synthesis during reflect, and consolidation in the background. 0.8.0 makes all of them observable.

- **On by default.** Per-bank LLM request tracing is recorded through an OpenTelemetry GenAI recorder and enabled out of the box, with a one-day retention window so traces don't accumulate unbounded.
- **In the Control Plane and the SDKs.** New `llm-requests` endpoints list traces and aggregate statistics, the Control Plane has a viewer for them, and the Python and TypeScript clients now expose the `tool_calls` and `llm_calls` trace from reflect so you can see exactly how an answer was produced.
- **Faithful recall traces.** Reciprocal-rank-fusion source ranks are preserved in the trace, so you can see which retrieval strategy surfaced each result.

If a retain looked wrong or a reflect answer surprised you, you can now open the trace and see the actual prompt, model, and tool calls behind it.

## Self-Maintaining Consolidation

Consolidation — the background process that turns raw facts into a clean, deduplicated set of observations and mental models — gets significantly more autonomous in 0.8.0.

- **Semantic de-duplication, on by default.** Near-duplicate observations are now detected by semantic similarity (default threshold 0.97) on both the create and update paths and merged instead of accumulating. This was the residual source of slowly drifting duplicate observations, and it's now closed out of the box.
- **A self-healing maintenance loop.** A background maintenance loop periodically reconciles banks that need consolidation and applies cross-tenant retention on a schedule, using server-side routines that scale to many tenants without a query storm. Banks stay clean without manual `consolidate` calls.
- **Bounded output.** Consolidation now runs under an explicit output-token budget, so a single large bank can't run away with an oversized generation.

## Resumable Long-Running Operations

Consolidation and batch retain can run for a while on large inputs. Previously, a worker restart mid-operation meant starting over. 0.8.0 adds a **durable progress snapshot**: long-running operations checkpoint their progress and resume from where they stopped, so a deploy or a transient crash no longer throws away completed work.

## Scalable Memory History

Mental-model and observation history used to live in a single JSONB column that grew without bound — on a busy bank it could balloon and eventually destabilize the row. 0.8.0 moves both into **dedicated history tables**, where each revision is its own row. This removes the growth ceiling, keeps the hot path lean, and makes history queryable. The behaviour is configurable, and a migration moves existing history across automatically.

## Provider Prompt Caching

Hindsight now uses **provider prompt-prefix caching** for the LLM calls behind retain, consolidation, and reflect. The shared, bank-agnostic prefixes of these prompts are cached at the provider, cutting both token cost and latency on repeated calls. It's enabled by default and requires no configuration.

## New Integrations

- **Claude Agent SDK** — a new integration that gives agents built on Anthropic's Claude Agent SDK persistent Hindsight memory.
- **Superagent** — a safety middleware integration for routing Superagent interactions through Hindsight.

## Other Notable Changes

- **Faster temporal recall.** The temporal entry-point scan is now bounded to the top matches per fact type, fixing a pathological slowdown on banks with many same-dated memories.
- **More LLM flexibility.** `HINDSIGHT_API_LLM_STRICT_SCHEMA` is honored across all JSON-schema-capable providers, `HINDSIGHT_API_LLM_EXTRA_BODY` is applied across all API providers, and the default MiniMax model is upgraded to M3.
- **ONNX local embeddings.** A new ONNX-based local embeddings provider joins the existing local and TEI options.
- **Tunable retrieval.** Per-strategy retrieval boosts (`semantic`, `bm25`, `graph`, `temporal`) and a configurable `HINDSIGHT_API_SEMANTIC_MIN_SIMILARITY` floor let you tune recall to your corpus.
- **External Postgres fixes.** VectorChord catalogs are added to the `search_path` so vchord works on externally managed PostgreSQL, and embedding dimensions are validated before pgvector writes to catch mismatches early.
- **Oracle 23ai backend.** Recall and mental-model history now work on the Oracle backend, and document lock/upsert is dialect-aware.
- **Robust BM25.** PGroonga BM25 query text is escaped so special characters no longer break keyword search.
- **More reliable model init.** Model initialization fails fast instead of hanging forever when a provider blocks, and `tool_choice="required"` is downgraded for servers that silently drop it.
- **Leaner reflect.** A fresh mental model can short-circuit forced retrieval, avoiding an extra LLM call.
- **Retain integrity.** Retain runs a pre-extraction freshness recheck and serializes concurrent writers to the same document, stops the bank routing key from polluting fact attribution, and surfaces richer per-item outcome metadata.
- **Control Plane polish.** Operations and graph legends are localized, and disabled audit-log / LLM-request views show a clear "not enabled" splash instead of an error.
- **Config correctness.** Bank configuration `PATCH` now persists for banks that have never been retained to.
