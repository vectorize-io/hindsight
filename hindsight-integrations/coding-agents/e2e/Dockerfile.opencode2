FROM hindsight-coding-agents-e2e-base
ARG VERSION=2.0.3
# The stable package publishes both `opencode` and `opencode2` binary aliases. Keep using the
# latter here so the shared E2E can install and exercise v1 and v2 side by side.
RUN npm install --global "@opencode/cli@${VERSION}"
