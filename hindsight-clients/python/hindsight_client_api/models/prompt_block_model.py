# coding: utf-8

"""
    Hindsight HTTP API

    HTTP API for Hindsight

    The version of the OpenAPI document: 0.9.2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PromptBlockModel(BaseModel):
    """
    One block of a message: its text, and the setting that decides it.  The **active** blocks of a message concatenate back to the exact text sent, so a client can render them separately without showing the reader something the model never receives. An **inactive** block has no text: it marks a setting that is switched off, at the point where it would land if it were on, with `note` explaining what turning it on would do.
    """ # noqa: E501
    label: StrictStr = Field(description="Human-readable name for the block.")
    text: StrictStr = Field(description="The block's text; empty when the block is inactive.")
    source: StrictStr = Field(description="`config` — a setting the reader can change (`field` names it); `builtin` — Hindsight's own fixed wording; `runtime` — a stand-in for the data the operation would be given.")
    var_field: Optional[StrictStr] = Field(default='', description="Config field behind this block; empty for pure runtime data.", alias="field")
    active: Optional[StrictBool] = Field(default=True, description="Whether this block is in the prompt as configured.")
    note: Optional[StrictStr] = None
    value: Optional[StrictStr] = None
    kind: StrictStr = Field(description="Shape of the value, so a client can offer the right control. `complex` means the value has structure this flattens for display and must be edited by its own dedicated UI.")
    choices: Optional[List[StrictStr]] = None
    editable: Optional[StrictBool] = Field(default=False, description="Whether this bank may override the field via the bank config API. Server-level fields shape the prompt but cannot be set per bank, and offering to edit one would only collect a 400.")
    __properties: ClassVar[List[str]] = ["label", "text", "source", "field", "active", "note", "value", "kind", "choices", "editable"]

    @field_validator('source')
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['config', 'builtin', 'runtime']):
            raise ValueError("must be one of enum values ('config', 'builtin', 'runtime')")
        return value

    @field_validator('kind')
    def kind_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['text', 'boolean', 'choice', 'complex']):
            raise ValueError("must be one of enum values ('text', 'boolean', 'choice', 'complex')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PromptBlockModel from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if note (nullable) is None
        # and model_fields_set contains the field
        if self.note is None and "note" in self.model_fields_set:
            _dict['note'] = None

        # set to None if value (nullable) is None
        # and model_fields_set contains the field
        if self.value is None and "value" in self.model_fields_set:
            _dict['value'] = None

        # set to None if choices (nullable) is None
        # and model_fields_set contains the field
        if self.choices is None and "choices" in self.model_fields_set:
            _dict['choices'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PromptBlockModel from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "label": obj.get("label"),
            "text": obj.get("text"),
            "source": obj.get("source"),
            "field": obj.get("field") if obj.get("field") is not None else '',
            "active": obj.get("active") if obj.get("active") is not None else True,
            "note": obj.get("note"),
            "value": obj.get("value"),
            "kind": obj.get("kind"),
            "choices": obj.get("choices"),
            "editable": obj.get("editable") if obj.get("editable") is not None else False
        })
        return _obj


